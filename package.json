{
  "name": "popoto-examples",
  "version": "0.0.1",
  "scripts": {
    "build": "mkdir gh-pages && cp -r auto-complete gh-pages",
    "pretest": "npm run build"
  },
  "dependencies": {
    "popotojs": "latest",
    "jquery": "latest",
    "d3": "latest"
  }
}
